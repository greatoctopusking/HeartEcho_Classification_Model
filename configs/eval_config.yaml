# 数据配置
data: "CACTUS/Images Dataset"
camus_data: "CAMUS"
num_workers: 4

# 模型配置
num_classes: 7
batch_size: 32

# 评估配置
test_only: false
val_only: false
full_data: true
plot_cm: true
plot_roc: true
save_dir: "results"
fold: 0

# 随机种子
seed: 42
